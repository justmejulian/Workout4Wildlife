---
import SignUpButton from '@components/SignUpButton';
import Overlay from './Overlay.astro';

interface Props {
  showSignUp?: boolean;
}

const { showSignUp = false }: Props = Astro.props;
---

<div id="hero">
  <Overlay title={'Thank you!'} subTitle={'We raised over $6300'} />

  <p class="prize">
    {'Win a '}<span>{'$15,000 '}</span>{
      ' trip to Namibia* and experience conservation'
    }
    <br />
    {' first hand.'}
  </p>
  {showSignUp && <SignUpButton client:load />}
  <p class="dates">
    {'Workout announcement '}<span>{'April 14'}</span>
    <br />
    {'Workout submission '}<span>{'April 24'}</span>
    <br />
    {'Participation fee '}<span>{'$20'}</span>
  </p>
  <a
    class="tcs"
    href="/Workout4Wildlife-Terms&Conditions-20230330.pdf"
    target="_blank"
  >
    {'*T&Cs Apply'}
  </a>
</div>

<style>
  #hero {
    position: relative;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    overflow: hidden;

    background: url('../images/hero-desktop.webp'),
      url('../images/hero-desktop-10.webp');
    background-position: center;
    background-size: cover;
  }

  p {
    font-family: 'Satoshi-Regular';
    font-size: calc(var(--base-font-size) * 2);
    max-width: calc(var(--base-font-size) * 33);
    text-align: center;
    color: #ffffff;
    margin: calc(var(--base-font-size) * 4) 0px;
  }

  .dates {
    margin-bottom: calc(var(--base-font-size) * 2);
  }

  .tcs {
    margin: 0px;
    font-size: var(--base-font-size);
    margin-bottom: calc(var(--base-font-size) * 0.5);
    color: #ffffff;
    text-decoration: none;
  }

  a:hover {
    text-decoration: underline;
  }

  span {
    font-family: 'Caveat-Bold';
    font-weight: 700;
    font-size: calc(var(--base-font-size) * 4);
    color: #fee878;
  }

  @media only screen and (max-width: 970px) {
    #hero {
      background: url('../images/hero-mobile.webp'),
        url('../images/hero-mobile-10.webp');
      background-position: center;
      background-size: cover;
    }

    p {
      font-size: calc(var(--base-font-size) * 1);
      max-width: calc(var(--base-font-size) * 20);
    }

    span {
      font-size: calc(var(--base-font-size) * 2);
    }

    .prize {
      order: -2;
      margin-top: calc(var(--base-font-size) * 1);
      margin-bottom: calc(var(--base-font-size) * 2);
    }

    .dates {
      order: -1;
      margin-top: 0px;
      margin-bottom: calc(var(--base-font-size) * 6);
    }

    .tcs {
      margin-top: calc(var(--base-font-size) * 2);
    }
  }
</style>
